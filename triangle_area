#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main(void)
{
	int base, height;
	double area;

	printf("삼각형의 밑변");
	scanf("%d", &base);
	printf("삼각형의 높이");
	scanf("%d", &height);

	area = 0.5 * height * base;
	printf("삼각형의 넓이 : %lf", area);

}
